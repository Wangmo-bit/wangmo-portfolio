---
import profile from '../content/profile.json';

interface Props {
  lang?: 'zh' | 'en';
}

const { lang = 'zh' } = Astro.props;
const isZh = lang === 'zh';
const name = isZh ? profile.name_zh : profile.name_en;
const tags = isZh ? profile.tags_zh : profile.tags_en;
const bio = (isZh ? profile.bio_zh : profile.bio_en).replace('\n', '<br>');
const altLangUrl = isZh ? '/en/' : '/';
---

<aside class="left-panel" id="leftPanel">
  <!-- Background glow layers -->
  <div class="glow-full"></div>
  <div class="glow-center"></div>
  <div class="glow-bottom"></div>
  <div class="glow-edge"></div>

  <div class="left-content">
    <!-- Avatar -->
    <div class="avatar-wrap">
      <div class="avatar-glow" id="avatarGlow"></div>
      <div class="avatar-ring" id="avatarRing"></div>
      <div class="avatar">MW</div>
    </div>

    <!-- Name -->
    <div class="name-block">
      <div class="name-primary">{name}</div>
      <div class="name-sub">{profile.domain}</div>
    </div>

    <!-- Bio tags: 1 top + 3 bottom -->
    <div class="bio-tags">
      <div class="bio-tags-row">
        <span class="bio-tag">{tags[0]}</span>
      </div>
      <div class="bio-tags-row">
        {tags.slice(1).map((tag: string) => (
          <span class="bio-tag">{tag}</span>
        ))}
      </div>
    </div>

    <!-- Bio -->
    <div class="bio" set:html={bio} />

    <!-- Social links -->
    <div class="social-links">
      <a class="social-btn" href={profile.social.jike} target="_blank" rel="noopener" data-label="即刻" title="即刻">
        <svg viewBox="0 0 24 24"><path d="M15.5 3h-1.2C11.5 3 9.2 5.3 9.2 8.1v6.3c0 1.7-1.4 3.1-3.1 3.1H5.5c-.4 0-.6.5-.3.8l.6.6c.5.5 1.1.7 1.8.7h.5c3 0 5.4-2.4 5.4-5.4V8c0-1.5 1.2-2.7 2.7-2.7h.5c.5 0 .8-.4.8-.8v-0.7c0-.4-.4-.8-.8-.8z" fill="currentColor"/></svg>
      </a>
      <a class="social-btn" href={profile.social.xiaohongshu} target="_blank" rel="noopener" data-label="小红书" title="小红书">
        <svg viewBox="0 0 200 200"><path d="M30.36 89.3h15.66l-4.58-19.56h-6.5L30.36 89.3zm-2.9 14.47h21.74L42 132.16H28.38l-.92-28.4zm94.67-34.03h-15.2l4.22 19.56h6.73l4.25-19.56zm2.44 34.03H103l7.22 28.4h13.38l.97-28.4zM100 0C44.77 0 0 44.77 0 100s44.77 100 100 100 100-44.77 100-100S155.23 0 100 0zm40.3 62.42L134.12 89.3h11.27v14.47h-14.38l-3.2 28.4h-13.84l-.75-28.4H99.54v19.1c0 5.07-.5 8.7-2.88 11.36-2.38 2.66-5.72 3.77-10.75 3.77h-8.53l-2.16-14.47h7.44c2.06 0 3.4-.47 4.03-1.22.62-.75.72-2.06.72-4.34v-14.2H70.16l-.66-28.4H54.62V89.3h11.32L59.7 62.42h16l4.14 16.08 1.88 10.8h.56l1.97-10.8 4.33-16.08h15.46v26.88h6.03V62.42h15.88l4.42 16.08 1.88 10.8h.56l1.69-10.8 4.42-16.08h15.47z" fill="currentColor"/></svg>
      </a>
      <a class="social-btn" href={profile.social.github} target="_blank" rel="noopener" data-label="GitHub" title="GitHub">
        <svg viewBox="0 0 24 24"><path d="M12 2C6.477 2 2 6.477 2 12c0 4.42 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.008-.866-.013-1.7-2.782.604-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.463-1.11-1.463-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.087 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0112 6.836c.85.004 1.705.114 2.504.336 1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48C19.138 20.161 22 16.416 22 12c0-5.523-4.477-10-10-10z" fill="currentColor"/></svg>
      </a>
    </div>

    <!-- Language switch -->
    <div class="lang-switch">
      <a class:list={['lang-btn', { active: isZh }]} href="/" onclick="localStorage.setItem('lang','zh')">中文</a>
      <a class:list={['lang-btn', { active: !isZh }]} href="/en/" onclick="localStorage.setItem('lang','en')">EN</a>
    </div>
  </div>
</aside>

<style>
  .left-panel {
    width: 420px;
    min-width: 380px;
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 60px 48px;
    overflow: hidden;
    background: var(--color-bg-deep);
  }

  /* Background glow layers */
  .glow-full {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 25% 15%, var(--current-theme, #00F0FF), transparent 55%),
                radial-gradient(ellipse at 75% 85%, var(--current-theme, #00F0FF), transparent 45%);
    opacity: 0.16;
    transition: all 1.2s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
  }

  .glow-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 180%;
    height: 180%;
    background: radial-gradient(circle at 50% 50%, var(--current-theme, #00F0FF), transparent 45%);
    opacity: 0.08;
    transition: all 1.2s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
    filter: blur(40px);
  }

  .glow-bottom {
    position: absolute;
    bottom: -5%;
    left: 50%;
    transform: translateX(-50%);
    width: 140%;
    height: 45%;
    background: radial-gradient(ellipse at 50% 100%, var(--current-theme, #00F0FF), transparent 55%);
    opacity: 0.2;
    transition: all 1.2s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
    filter: blur(20px);
  }

  .glow-edge {
    position: absolute;
    top: 0;
    right: 0;
    width: 2px;
    height: 100%;
    background: linear-gradient(180deg, transparent 5%, var(--current-theme, #00F0FF) 50%, transparent 95%);
    opacity: 0.45;
    transition: all 1.2s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
  }

  .left-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    max-width: 300px;
  }

  /* Avatar */
  .avatar-wrap {
    position: relative;
    width: 110px;
    height: 110px;
  }

  .avatar {
    width: 110px;
    height: 110px;
    border-radius: 50%;
    background: linear-gradient(135deg, #1a1a2e, #13131f);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 36px;
    font-weight: 600;
    color: var(--color-text-secondary);
    position: relative;
    z-index: 1;
    overflow: hidden;
    border: 1.5px solid color-mix(in srgb, var(--current-theme, #00F0FF) 30%, transparent);
    transition: border-color 1s ease;
  }

  .avatar-glow {
    position: absolute;
    inset: -16px;
    border-radius: 50%;
    background: radial-gradient(circle, var(--current-theme, #00F0FF), transparent 70%);
    opacity: 0.15;
    animation: avatar-breathe 4s ease-in-out infinite;
    transition: background 1s ease;
    pointer-events: none;
  }

  .avatar-ring {
    position: absolute;
    inset: -6px;
    border-radius: 50%;
    border: 1px solid var(--current-theme, #00F0FF);
    opacity: 0;
    animation: avatar-ring-breathe 4s ease-in-out infinite;
    transition: border-color 1s ease;
    pointer-events: none;
  }

  /* Name */
  .name-block {
    text-align: center;
  }

  .name-primary {
    font-size: 30px;
    font-weight: 700;
    letter-spacing: -0.5px;
    line-height: 1.2;
  }

  .name-sub {
    font-size: 12px;
    color: var(--color-text-tertiary);
    margin-top: 6px;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 1px;
  }

  /* Bio tags */
  .bio-tags {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }

  .bio-tags-row {
    display: flex;
    gap: 8px;
    justify-content: center;
  }

  .bio-tag {
    font-size: 12px;
    padding: 5px 14px;
    border-radius: 100px;
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    color: var(--color-text-secondary);
    white-space: nowrap;
    transition: all 0.3s ease;
  }

  /* Bio */
  .bio {
    font-size: 13px;
    line-height: 1.9;
    color: var(--color-text-secondary);
    text-align: center;
  }

  /* Social */
  .social-links {
    display: flex;
    gap: 12px;
  }

  .social-btn {
    width: 42px;
    height: 42px;
    border-radius: 12px;
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.25s ease;
    position: relative;
    overflow: hidden;
    text-decoration: none;
  }

  .social-btn svg {
    width: 18px;
    height: 18px;
    fill: var(--color-text-secondary);
    transition: fill 0.25s ease;
  }

  .social-btn::after {
    content: attr(data-label);
    position: absolute;
    bottom: -24px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 10px;
    color: var(--color-text-tertiary);
    opacity: 0;
    transition: all 0.2s ease;
    white-space: nowrap;
    pointer-events: none;
  }

  .social-btn:hover {
    border-color: var(--current-theme, #00F0FF);
    transform: translateY(-3px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3), 0 0 20px color-mix(in srgb, var(--current-theme, #00F0FF) 10%, transparent);
  }

  .social-btn:hover svg { fill: var(--current-theme, #00F0FF); }
  .social-btn:hover::after { opacity: 1; bottom: -22px; }

  /* Language switch */
  .lang-switch {
    display: flex;
    align-items: center;
    font-size: 12px;
    margin-top: 4px;
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    overflow: hidden;
  }

  .lang-btn {
    padding: 6px 16px;
    cursor: pointer;
    transition: all 0.3s ease;
    color: var(--color-text-tertiary);
    user-select: none;
    text-decoration: none;
  }

  .lang-btn.active {
    background: color-mix(in srgb, var(--current-theme, #00F0FF) 8%, transparent);
    color: var(--current-theme, #00F0FF);
  }

  .lang-btn:not(.active):hover {
    color: var(--color-text-secondary);
    background: rgba(255, 255, 255, 0.02);
  }

  /* Tablet */
  @media (max-width: 1023px) and (min-width: 768px) {
    .left-panel {
      width: 320px;
      min-width: 280px;
      padding: 40px 32px;
    }

    .left-content {
      gap: 16px;
    }

    .avatar-wrap, .avatar {
      width: 90px;
      height: 90px;
    }

    .avatar { font-size: 28px; }

    .name-block .name-primary { font-size: 24px; }

    .bio-tag { font-size: 11px; padding: 4px 12px; }
    .bio { font-size: 12px; }
  }

  /* Mobile */
  @media (max-width: 767px) {
    .left-panel {
      width: 100%;
      min-width: unset;
      padding: 40px 24px 32px;
      border-right: none;
      border-bottom: 2px solid var(--current-theme, #00F0FF);
      position: relative;
    }

    .glow-edge {
      /* Move from right edge to bottom */
      top: auto;
      right: auto;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 2px;
      background: linear-gradient(90deg, transparent 5%, var(--current-theme, #00F0FF) 50%, transparent 95%);
    }

    .left-content {
      gap: 14px;
      max-width: 100%;
    }

    .avatar-wrap, .avatar {
      width: 80px;
      height: 80px;
    }

    .avatar { font-size: 24px; }
    .avatar-glow { inset: -12px; }
    .avatar-ring { inset: -4px; }

    .name-block .name-primary {
      font-size: 20px;
    }

    .name-sub { font-size: 11px; }

    .bio-tags-row { gap: 6px; }
    .bio-tag {
      font-size: 10px;
      padding: 4px 10px;
    }

    .bio {
      font-size: 12px;
      line-height: 1.7;
    }

    .social-links { gap: 10px; }
    .social-btn {
      width: 38px;
      height: 38px;
      border-radius: 10px;
    }
    .social-btn svg {
      width: 16px;
      height: 16px;
    }
    .social-btn::after { display: none; }

    .lang-switch { font-size: 11px; }
    .lang-btn { padding: 5px 14px; }
  }
</style>
